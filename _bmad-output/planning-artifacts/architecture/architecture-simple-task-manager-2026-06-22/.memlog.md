---
scope: Simple Task Manager API (NestJS) - whole system
purpose: build-substrate
altitude: feature
updated: 2026-06-22T22:44
---

- (version) Verified 2026-06-22 via npm: Node 24.x LTS, NestJS 11.x, TypeORM 1.0.0, @nestjs/typeorm 11.x, pg 8.x.
- (version) Verified 2026-06-22: PostgreSQL 18.x current stable (19 beta), @nestjs/swagger 11.x, @nestjs/jwt 11.x, passport-jwt 4.x, class-validator 0.15.x, bcrypt 6.x.
- (constraint) Stack locked by PRD/brief: NestJS + REST + PostgreSQL + TypeORM + JWT (inherited, read-only).
- (constraint) Auth: short-lived JWT (~1h), no refresh token in v1 (PRD FR-2).
- (constraint) Error envelope statusCode/message/error and offset/limit pagination are PRD invariants (FR-11/FR-6).
- (constraint) Deployment out of scope v1: local Docker Compose only (PRD non-goal).
- (decision) AD-1 Paradigm: layered modular monolith. Binds all modules. Prevents logic-in-controllers/DB-in-controllers. Rule: Controller->Service->Repository per NestJS module. [ADOPTED]
- (decision) AD-2 Module boundaries + dependency direction. Binds Auth/Users/Tasks/Health/Common. Prevents circular deps. Rule: features depend on Common and Users(identity) only, via exported providers; no cross-module repo imports.
- (decision) AD-3 State mutation via TypeORM repositories + migrations only. Prevents schema drift/scattered SQL. Rule: entities are sole schema source; no synchronize outside tests; changes via migrations.
- (decision) AD-4 Task ownership scoping (key security invariant). Binds all Task ops. Prevents cross-user access. Rule: every Task query/command filters ownerId=currentUser.id at service/repo layer; cross-user -> 404. [ADOPTED FR-9/NFR-1]
- (decision) AD-5 Cross-cutting concerns global+centralized. Prevents per-controller divergence. Rule: one global JwtAuthGuard (public via @Public), one global ValidationPipe (whitelist+forbidNonWhitelisted+transform), one global exception filter emitting statusCode/message/error.
- (decision) AD-6 Identity ownership. Rule: UsersModule owns User entity + password hashing; AuthModule owns JWT issue/verify; other modules get user via @CurrentUser, never read credentials.
- (decision) AD-7 Config + secrets via @nestjs/config env only. Prevents hardcoded secrets/env drift. Rule: all config from env/.env; required env validated at boot; nothing secret committed. [ADOPTED NFR-7]
- (decision) AD-8 Response/error/pagination contracts. Rule: error=statusCode/message/error; list=offset/limit (default 20,max 100) returning data+total+limit+offset; REST+Swagger at /docs. [ADOPTED PRD]
- (assumption) AD-9 Primary keys = UUID v4 (consistent, non-enumerable -> supports 404-not-leak isolation). NEEDS CONFIRM (vs auto-increment int).
- (assumption) List response envelope shape data/total/limit/offset; logging = Nest default Logger. NEEDS CONFIRM.
- (event) Spine drafting (fast path): 9 ADs + conventions + verified stack + seed tree/ERD.
- (event) lint_spine.py: 0 findings (mechanically clean).
- (decision) Adversarial review closed 3 divergence holes: AD-8 validation errors -> single message string + sort=field:asc|desc default createdAt:desc; AD-6 @CurrentUser yields {id,email}.
- (event) spine finalized
